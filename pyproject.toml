[project]
name = "envu"
version = "0.1.0"
description = "Environment utilities including winget to scoop migration"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=13.0.0",
]
